#spring:
#  datasource:
#    driver-class-name: com.mysql.jdbc.Driver
#    url: jdbc:mysql://192.168.10.225:3306/cdt_plugs?useSSL=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull
#    username: ld225_rw
#    password: laidian225
#    # 一个连接的生命时长（毫秒），超时而且没被使用则被释放（retired），缺省:30分钟，建议设置比数据库超时时长少30秒以上
#    maxLifetime: 1765000
#    # 连接池中允许的最大连接数。缺省值：10；推荐的公式：((core_count * 2) + effective_spindle_count)
#    maximumPoolSize: 100
sharding:
  jdbc:
    datasource:
      names: testwrite,testread
      testwrite:
        type: com.zaxxer.hikari.HikariDataSource
        driver-class-name: com.mysql.jdbc.Driver
        jdbc-url: jdbc:mysql://127.0.0.1:3306/mytest?useSSL=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&userAffectRows=true
        username: root
        password: 123456
      testread:
        type: com.zaxxer.hikari.HikariDataSource
        driver-class-name: com.mysql.jdbc.Driver
        jdbc-url: jdbc:mysql://127.0.0.1:3306/mytest?useSSL=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&userAffectRows=true
        username: root
        password: 123456
    config:
      sharding:
        master-slave-rules:
          ds_0:
            master-data-source-name: testwrite
            slave-data-source-names: testread
        props:
          sql:
            show: true
